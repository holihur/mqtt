version: "2"
linters:
  default: none
  enable:
    - errcheck
    - govet
    - staticcheck
    - ineffassign
    - unused
    - funlen
    - gocyclo
    - gocognit
    - lll
    - cyclop
  settings:
    funlen:
      lines: 300
      statements: 200
    gocyclo:
      min-complexity: 70
    gocognit:
      min-complexity: 120
    lll:
      line-length: 320
    cyclop:
      max-complexity: 70
      package-average: 30.0
  exclusions:
    generated: lax
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
    paths:
      - broker_test.go
      - codec_test.go
    rules:
      - path: _test\.go
        linters:
          - errcheck
          - unused
          - staticcheck
      - path: internal/broker/broker.*\.go
        linters:
          - funlen
          - gocyclo
          - gocognit
          - cyclop
          - lll
formatters:
  enable:
    - gofmt
    - goimports
